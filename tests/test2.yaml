services:
  - name: foo
    ensure: "present"
    config:
      host: "foo.local"
    routes:
      - paths: "/"
        hosts: "api.foo.com"
    plugins:
      - name: cors
        config:
          origins: "*"
      - name: jwt
